name: Encrypt Gemini for Onepune

on:
  workflow_dispatch:

permissions: {}

jobs:
  encrypt:
    runs-on: ubuntu-latest
    steps:
      - name: Encrypt Gemini secret for Onepune
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          ONEPUNE_PUBLIC_KEY: Do8It/taZzj0SOaQ6ED5NqHRKog2Px4ELcqbwfRPLAE=
        run: |
          test -n "$GEMINI_API_KEY"
          python -m pip install --disable-pip-version-check --quiet PyNaCl==1.5.0
          python - <<'PY'
          import base64
          import os
          from pathlib import Path

          from nacl import encoding, public

          public_key = public.PublicKey(
              os.environ["ONEPUNE_PUBLIC_KEY"].encode("ascii"),
              encoding.Base64Encoder(),
          )
          encrypted = public.SealedBox(public_key).encrypt(
              os.environ["GEMINI_API_KEY"].encode("utf-8")
          )
          output = Path(os.environ["RUNNER_TEMP"]) / "onepune-gemini-secret.txt"
          output.write_text(base64.b64encode(encrypted).decode("ascii"), encoding="ascii")
          PY

      - name: Upload one-time encrypted payload
        uses: actions/upload-artifact@v4
        with:
          name: onepune-encrypted-gemini
          path: ${{ runner.temp }}/onepune-gemini-secret.txt
          if-no-files-found: error
          retention-days: 1
